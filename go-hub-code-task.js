import { appendEvidence } from "./go-hub-evidence-ledger.js";
import { deriveFactoryNextAction } from "./go-hub-factory-authority.js";

const NEXT_ACTION = Object.freeze({
  INSPECTING: "inspect", BRANCH_READY: "edit", EDITING: "review-diff", DIFF_REVIEWED: "test",
  TESTED_OR_TEST_DEFERRED: "commit", COMMITTED: "open-pr", PR_OPEN: "check-ci", CI_RUNNING: "check-ci",
  CI_GREEN: "merge", CI_FAILED: "fix-ci", MERGED: "track-deploy", DEPLOYING: "track-deploy",
  DEPLOYED: "verify", VERIFIED: "complete", BLOCKED: "resolve-blocker", CONFLICT: "resolve-conflict",
  DEPLOY_FAILED: "fix-deploy", VERIFY_FAILED: "fix-verify", ROLLBACK_IN_PROGRESS: "continue-rollback",
});
const PRODUCTION_ADVANCE = Object.freeze({ INSPECT_REALITY: "BASELINE", BASELINE: "TRACE", TRACE: "PLAN", PLAN: "WRITE", LOCAL_VERIFY: "PIECE_READY" });

function clone(value) { return JSON.parse(JSON.stringify(value)); }
function now() { return new Date().toISOString(); }
function effectiveNextAction(state) { return deriveFactoryNextAction(state) || NEXT_ACTION[state.state]; }
function requiredString(value, label) { const normalized=String(value||"").trim(); if(!normalized) throw new Error(`${label} is required`); return normalized; }
function currentProductArtifact(state){ return String(state.buildArtifact?.kind||"").toLowerCase()==="apk" ? state.signedArtifact : state.buildArtifact; }

function normalizeInitial(initial={}) {
  return { id:String(initial.id||""), intent:String(initial.intent||""), repository:String(initial.repository||""), state:"INSPECTING", nextAction:"inspect",
    baseBranch:null, baseSha:null, workBranch:null, headSha:null, touchedPaths:[], diffFingerprint:null, blocker:null, pullRequest:null, ci:null, merge:null,
    deployment:null, verification:null, rollback:null, mission:null, blueprint:null, currentPiece:null, evidence:[], factoryStage:null,
    workPackage:null, productionPhase:null, productionTrace:[], piece:null, pieceQc:null, gateHandoff:null, assembly:null, assemblyQc:null, mergeGate:null,
    buildArtifact:null, signingGate:null, signedArtifact:null, productQc:null, verificationScan:null, closeout:null, lessons:[],
    audit:[{at:now(),event:"TASK_CREATED",state:"INSPECTING"}] };
}

function transitionState(current,nextState,evidence={}){
  const state=String(nextState||""); if(!NEXT_ACTION[state]) throw new Error(`unsupported state: ${state}`);
  const next=clone(current), priorHead=next.headSha, incomingHead=evidence.headSha==null?priorHead:String(evidence.headSha);
  if(state==="PR_OPEN"){const h=evidence.pullRequest?.headSha==null?null:String(evidence.pullRequest.headSha); if(!incomingHead||!h||h!==incomingHead) throw new Error("pull request head SHA does not match current head");}
  if(["CI_RUNNING","CI_GREEN","CI_FAILED"].includes(state)){const h=evidence.ci?.headSha==null?null:String(evidence.ci.headSha); if(!incomingHead||!h||h!==incomingHead) throw new Error("CI head SHA does not match current head");}
  if(state==="DEPLOYED"&&evidence.deployment?.status!=="success") throw new Error("successful deployment evidence is required");
  if(state==="VERIFIED"){const v=evidence.verification;if(!v||v.status!=="success"||!v.kind||!v.target||v.evidence==null||!v.timestamp) throw new Error("successful verification evidence is required");}
  if(state==="ROLLBACK_IN_PROGRESS"){const expected={EDITING:"discard-pending-edits",COMMITTED:"reset-work-branch",MERGED:"revert-merge"}[current.state];if(!expected||evidence.rollback?.kind!==expected)throw new Error("rollback kind does not match current state");}
  next.state=state; next.nextAction=NEXT_ACTION[state];
  for(const key of ["baseBranch","baseSha","workBranch","headSha","blocker"]){if(Object.hasOwn(evidence,key))next[key]=evidence[key]==null?null:String(evidence[key]);}
  if(Object.hasOwn(evidence,"touchedPaths")) next.touchedPaths=Array.isArray(evidence.touchedPaths)?[...evidence.touchedPaths]:[];
  for(const key of ["pullRequest","ci","merge","deployment","verification","rollback"]){if(Object.hasOwn(evidence,key))next[key]=evidence[key]==null?null:clone(evidence[key]);}
  if(!Object.hasOwn(evidence,"blocker")&&!['BLOCKED','CONFLICT'].includes(state))next.blocker=null;
  if(priorHead&&incomingHead&&priorHead!==incomingHead){next.diffFingerprint=null;next.pullRequest=null;next.ci=null;next.merge=null;next.deployment=null;next.verification=null;next.rollback=null;}
  if(state==="DIFF_REVIEWED"){if(!incomingHead)throw new Error("headSha is required for DIFF_REVIEWED");next.headSha=incomingHead;next.diffFingerprint=String(evidence.diffFingerprint||"");if(!next.diffFingerprint)throw new Error("diffFingerprint is required for DIFF_REVIEWED");}
  next.audit.push({at:now(),event:"STATE_TRANSITION",from:current.state,to:state,headSha:next.headSha}); return next;
}

function normalizeSnapshot(value){
  const state=clone(value); if(!state||typeof state!=="object"||Array.isArray(state))throw new Error("task snapshot is required");
  if(!NEXT_ACTION[state.state])throw new Error("task snapshot state is invalid"); if(!Array.isArray(state.audit))throw new Error("task snapshot audit is required");
  for(const key of ["mission","blueprint","currentPiece","workPackage","piece","pieceQc","gateHandoff","assembly","assemblyQc","mergeGate","buildArtifact","signingGate","signedArtifact","productQc","verificationScan","closeout"]){state[key]=state[key]==null?null:clone(state[key]);}
  state.evidence=Array.isArray(state.evidence)?clone(state.evidence):[]; state.lessons=Array.isArray(state.lessons)?clone(state.lessons):[];
  state.factoryStage=state.factoryStage==null?null:String(state.factoryStage); state.productionPhase=state.productionPhase==null?null:String(state.productionPhase); state.productionTrace=Array.isArray(state.productionTrace)?clone(state.productionTrace):[];
  if(!state.factoryStage&&state.nextAction!==NEXT_ACTION[state.state])throw new Error("task snapshot state is invalid"); state.nextAction=effectiveNextAction(state); return state;
}

function setWorkPackageState(current,input={}){
  const blueprintRef=requiredString(input.blueprintRef,"work package blueprintRef"); if(!current.blueprint?.ref||String(current.blueprint.ref)!==blueprintRef)throw new Error("work package must match the mounted blueprint reference");
  const next=clone(current); next.workPackage={id:requiredString(input.id,"work package id"),title:requiredString(input.title,"work package title"),purpose:requiredString(input.purpose,"work package purpose"),blueprintRef,inputs:Array.isArray(input.inputs)?clone(input.inputs):[],expectedOutputs:Array.isArray(input.expectedOutputs)?clone(input.expectedOutputs):[],dependencies:Array.isArray(input.dependencies)?clone(input.dependencies):[],assemblyTarget:requiredString(input.assemblyTarget,"work package assemblyTarget")};
  next.currentPiece={id:next.workPackage.id,title:next.workPackage.title,purpose:next.workPackage.purpose}; next.factoryStage="PRODUCTION";next.productionPhase="INSPECT_REALITY";next.productionTrace=[];next.piece=null;next.pieceQc=null;next.gateHandoff=null;next.audit.push({at:now(),event:"WORK_PACKAGE_STARTED",workPackageId:next.workPackage.id});return next;
}
function recordProductionStepState(current,input={}){
  if(current.factoryStage!=="PRODUCTION"||!current.workPackage)throw new Error("active Production work package is required"); const expected=String(current.productionPhase||"INSPECT_REALITY"), step=requiredString(input.step,"Production step"); if(step!==expected)throw new Error(`Production sequence expected ${expected} before ${step}`); const evidence=input.evidence&&typeof input.evidence==="object"&&!Array.isArray(input.evidence)?clone(input.evidence):{};
  if(step==="INSPECT_REALITY"){const repository=requiredString(evidence.repository,"Inspect Reality repository");requiredString(evidence.headSha,"Inspect Reality headSha");if(current.repository&&repository!==current.repository)throw new Error("Inspect Reality repository must match the active task repository");}
  else if(step==="BASELINE"){const baseSha=requiredString(evidence.baseSha,"Baseline baseSha"), inspectHead=current.productionTrace?.find(i=>i.step==="INSPECT_REALITY")?.evidence?.headSha;if(inspectHead&&baseSha!==inspectHead)throw new Error("Baseline must match the inspected Reality head");}
  else if(step==="TRACE")requiredString(evidence.summary,"Trace summary"); else if(step==="PLAN"){if(requiredString(evidence.blueprintRef,"Plan blueprintRef")!==current.blueprint?.ref)throw new Error("Plan must match the mounted Blueprint");requiredString(evidence.planRef,"Plan planRef");}
  else if(step==="LOCAL_VERIFY"){if(!current.piece)throw new Error("active Piece is required before Local Verify");if(evidence.status!=="pass")throw new Error("Local Verify pass is required before Piece QC");if(String(evidence.headSha||"")!==current.piece.headSha)throw new Error("Local Verify must match the active Piece head");}
  else if(step==="WRITE")throw new Error("WRITE is completed by recording the Piece"); else throw new Error(`unsupported Production step: ${step}`);
  const nextPhase=PRODUCTION_ADVANCE[step];if(!nextPhase)throw new Error(`Production step cannot advance: ${step}`); const next=clone(current);next.productionTrace=Array.isArray(next.productionTrace)?next.productionTrace:[];next.productionTrace.push({step,evidence,recordedAt:now()});next.productionPhase=nextPhase;next.audit.push({at:now(),event:"PRODUCTION_STEP_RECORDED",step,nextPhase});return next;
}
function recordPieceState(current,input={}){if(!current.workPackage)throw new Error("active work package is required");if(current.factoryStage!=="PRODUCTION"||current.productionPhase!=="WRITE")throw new Error("Production sequence requires WRITE before recording the Piece");const workPackageId=requiredString(input.workPackageId,"piece workPackageId");if(workPackageId!==current.workPackage.id)throw new Error("piece must match the active work package");const next=clone(current);next.piece={id:requiredString(input.id,"piece id"),workPackageId,repository:requiredString(input.repository,"piece repository"),branch:requiredString(input.branch,"piece branch"),headSha:requiredString(input.headSha,"piece headSha"),changedPaths:Array.isArray(input.changedPaths)?clone(input.changedPaths):[],outputs:Array.isArray(input.outputs)?clone(input.outputs):[]};next.factoryStage="PRODUCTION";next.productionPhase="LOCAL_VERIFY";next.productionTrace.push({step:"WRITE",evidence:{repository:next.piece.repository,branch:next.piece.branch,headSha:next.piece.headSha,changedPaths:clone(next.piece.changedPaths)},recordedAt:now()});next.pieceQc=null;next.gateHandoff=null;next.audit.push({at:now(),event:"PIECE_RECORDED",pieceId:next.piece.id,headSha:next.piece.headSha});return next;}
function recordPieceQcState(current,input={}){if(!current.piece)throw new Error("active Piece is required before Piece QC");if(current.productionPhase!=="PIECE_READY")throw new Error("Local Verify pass is required before Piece QC");const status=String(input.status||"");if(!["pass","fail"].includes(status))throw new Error("Piece QC status must be pass or fail");if(String(input.checkedHeadSha||"")!==current.piece.headSha)throw new Error("Piece QC checked head must match the active Piece head");const next=clone(current);next.pieceQc={status,checkedHeadSha:current.piece.headSha,checks:input.checks&&typeof input.checks==="object"?clone(input.checks):{},evidenceIds:Array.isArray(input.evidenceIds)?input.evidenceIds.map(String):[],checkedAt:requiredString(input.checkedAt,"Piece QC checkedAt")};next.factoryStage="PIECE_QC";next.gateHandoff=null;next.audit.push({at:now(),event:"PIECE_QC_RECORDED",status});return next;}
function recordGateHandoffState(current,input={}){if(current.pieceQc?.status!=="pass")throw new Error("passed Piece QC is required before Ready Gate");if(!current.piece||current.pieceQc.checkedHeadSha!==current.piece.headSha)throw new Error("Ready Gate requires Piece QC for the active Piece head");const expected=current.pieceQc.evidenceIds,received=Array.isArray(input.evidenceIds)?input.evidenceIds.map(String):[];const exact=expected.length>0&&expected.length===received.length&&expected.every((id,i)=>id===received[i])&&expected.every(id=>current.evidence.some(item=>item?.id===id&&item?.scope==="piece"&&item?.headSha===current.piece.headSha));if(!exact)throw new Error("Ready Gate requires exact Piece QC evidence");if(input.status!=="READY_FOR_ASSEMBLY"||input.pieceId!==current.piece.id||input.workPackageId!==current.workPackage?.id||input.blueprintRef!==current.blueprint?.ref||input.headSha!==current.piece.headSha)throw new Error("Ready Gate handoff does not match current production truth");const next=clone(current);next.gateHandoff=clone(input);next.factoryStage="READY_GATE";next.audit.push({at:now(),event:"READY_GATE_RECORDED",status:String(input.status||"")});return next;}
function addEvidenceState(current,input={}){const next=clone(current);next.evidence=appendEvidence(next.evidence,input);const entry=next.evidence.at(-1);next.audit.push({at:now(),event:"EVIDENCE_RECORDED",evidenceId:entry.id,claim:entry.claim,headSha:entry.headSha});return next;}
function clearDownstream(next){next.productQc=null;next.verificationScan=null;next.closeout=null;next.lessons=[];}
function recordAssemblyState(current,input={}){if(current.gateHandoff?.status!=="READY_FOR_ASSEMBLY")throw new Error("Ready Gate handoff is required before Assembly");if(input.status!=="ASSEMBLED"||input.blueprintRef!==current.blueprint?.ref)throw new Error("Assembly must match the mounted Blueprint");const pieceIds=Array.isArray(input.pieceIds)?input.pieceIds.map(String):[];if(!pieceIds.includes(current.gateHandoff.pieceId))throw new Error("Assembly must include the Ready Gate Piece");const next=clone(current);next.assembly={...clone(input),id:requiredString(input.id,"Assembly id"),repository:requiredString(input.repository,"Assembly repository"),integrationBranch:requiredString(input.integrationBranch,"Assembly integrationBranch"),integrationHeadSha:requiredString(input.integrationHeadSha,"Assembly integrationHeadSha"),pieceIds,sourceHeads:Array.isArray(input.sourceHeads)?input.sourceHeads.map(String):[]};next.factoryStage="ASSEMBLY";next.assemblyQc=null;next.mergeGate=null;next.buildArtifact=null;next.signingGate=null;next.signedArtifact=null;clearDownstream(next);next.audit.push({at:now(),event:"ASSEMBLY_RECORDED",assemblyId:next.assembly.id,headSha:next.assembly.integrationHeadSha});return next;}
function recordAssemblyQcState(current,input={}){if(!current.assembly)throw new Error("Assembly is required before Assembly QC");if(!["pass","fail"].includes(input.status)||input.checkedHeadSha!==current.assembly.integrationHeadSha)throw new Error("Assembly QC must match the current Assembly head");const ids=Array.isArray(input.evidenceIds)?input.evidenceIds.map(String):[];if(input.status==="pass"&&(!ids.length||ids.some(id=>!current.evidence.some(item=>item?.id===id&&item?.scope==="assembly"&&item?.headSha===current.assembly.integrationHeadSha))))throw new Error("Assembly QC pass requires exact-head evidence");const next=clone(current);next.assemblyQc=clone(input);next.factoryStage="ASSEMBLY_QC";next.mergeGate=null;next.buildArtifact=null;next.signingGate=null;next.signedArtifact=null;clearDownstream(next);next.audit.push({at:now(),event:"ASSEMBLY_QC_RECORDED",status:input.status,headSha:input.checkedHeadSha});return next;}
function recordMergeGateState(current,input={}){if(current.assemblyQc?.status!=="pass"||current.assemblyQc.checkedHeadSha!==current.assembly?.integrationHeadSha)throw new Error("passed Assembly QC for the current head is required before Merge Gate");if(input.status!=="MERGED_VERIFIED"||input.assemblyId!==current.assembly?.id||input.sourceHeadSha!==current.assembly?.integrationHeadSha)throw new Error("Merge Gate must match the accepted Assembly");const pr=input.pullRequest||{},ci=input.ci||{},merge=input.merge||{},verification=input.postMergeVerification||{},assemblyHead=current.assembly.integrationHeadSha,prNumber=Number(pr.number||0),prHead=requiredString(pr.headSha,"Merge Gate pull request head"),ciHead=requiredString(ci.headSha,"Merge Gate CI head"),mergeHead=requiredString(merge.headSha,"Merge Gate merge head"),mergeSha=requiredString(merge.mergeSha,"Merge Gate merge SHA"),mainSha=requiredString(verification.mainSha,"Merge Gate main SHA");if(!prNumber||prHead!==assemblyHead||ci.status!=="success"||ciHead!==prHead||mergeHead!==prHead||Number(merge.pullRequestNumber||0)!==prNumber||verification.status!=="pass"||mainSha!==mergeSha||!String(verification.checkedAt||"").trim())throw new Error("Merge Gate requires exact PR CI merge and post-merge verification truth");const next=clone(current);next.pullRequest=clone(pr);next.ci=clone(ci);next.merge=clone(merge);next.mergeGate={status:"MERGED_VERIFIED",assemblyId:current.assembly.id,sourceHeadSha:assemblyHead,pullRequestNumber:prNumber,pullRequestHeadSha:prHead,ciHeadSha:ciHead,mergeSha,mainSha,checkedAt:String(verification.checkedAt)};next.factoryStage="MERGE_GATE";next.buildArtifact=null;next.signingGate=null;next.signedArtifact=null;clearDownstream(next);next.audit.push({at:now(),event:"MERGE_GATE_RECORDED",pullRequestNumber:prNumber,mainSha});return next;}
function recordBuildArtifactState(current,input={}){if(current.assemblyQc?.status!=="pass"||current.assemblyQc.checkedHeadSha!==current.assembly?.integrationHeadSha)throw new Error("passed Assembly QC for the current head is required before Build");if(current.mergeGate?.status!=="MERGED_VERIFIED"||current.mergeGate.assemblyId!==current.assembly?.id||current.mergeGate.sourceHeadSha!==current.assembly?.integrationHeadSha)throw new Error("verified Merge Gate for the current Assembly is required before Build");if(input.status!=="BUILT"||input.assemblyId!==current.assembly.id||input.sourceHeadSha!==current.mergeGate.mainSha||input.blueprintRef!==current.blueprint?.ref)throw new Error("Build Artifact must match the verified Merge Gate and mounted Blueprint");const next=clone(current);next.buildArtifact={...clone(input),id:requiredString(input.id,"Artifact id"),kind:requiredString(input.kind,"Artifact kind"),assemblyHeadSha:current.assembly.integrationHeadSha,digest:requiredString(input.digest,"Artifact digest"),location:requiredString(input.location,"Artifact location"),builtAt:requiredString(input.builtAt,"Artifact builtAt")};next.factoryStage="BUILD";next.signingGate=null;next.signedArtifact=null;clearDownstream(next);next.audit.push({at:now(),event:"BUILD_ARTIFACT_RECORDED",artifactId:next.buildArtifact.id,digest:next.buildArtifact.digest});return next;}
function recordSigningGateState(current,input={}){if(String(current.buildArtifact?.kind||"").toLowerCase()!=="apk")throw new Error("APK Build Artifact is required before Signing Gate");if(input.state!=="SIGNING_GATE_READY"||input.sourceArtifactId!==current.buildArtifact.id||input.unsignedApkSha256!==current.buildArtifact.digest||input.sourceSha!==current.buildArtifact.sourceHeadSha)throw new Error("Signing Gate must match the current APK Build Artifact");const next=clone(current);next.signingGate=clone(input);next.signedArtifact=null;next.factoryStage="SIGNING_GATE";clearDownstream(next);next.audit.push({at:now(),event:"SIGNING_GATE_RECORDED",artifactId:input.sourceArtifactId,profileId:input.signingProfileId||null});return next;}
function recordSignedArtifactState(current,input={}){if(current.signingGate?.state!=="SIGNING_GATE_READY")throw new Error("Signing Gate is required before signed Artifact verification");if(input.state!=="SIGNATURE_VERIFIED"||input.artifact?.status!=="SIGNATURE_VERIFIED"||input.artifact?.sourceArtifactId!==current.buildArtifact?.id||input.evidence?.unsignedApkSha256!==current.buildArtifact?.digest||input.evidence?.sourceSha!==current.buildArtifact?.sourceHeadSha||input.artifact?.digest!==input.evidence?.signedApkSha256)throw new Error("signed APK verification must match the current Signing Gate and Build Artifact");const next=clone(current);next.signedArtifact=clone(input.artifact);next.signatureEvidence=clone(input.evidence);next.factoryStage="SIGNATURE_VERIFIED";clearDownstream(next);next.audit.push({at:now(),event:"SIGNED_ARTIFACT_VERIFIED",artifactId:next.signedArtifact.id,digest:next.signedArtifact.digest});return next;}
function recordProductQcState(current,input={}){const artifact=currentProductArtifact(current);if(String(current.buildArtifact?.kind||"").toLowerCase()==="apk"&&current.factoryStage!=="SIGNATURE_VERIFIED")throw new Error("Signing Gate and verified signed APK are required before Product QC");if(!artifact||input.artifactId!==artifact.id||input.artifactDigest!==artifact.digest)throw new Error("Product QC must match the current Artifact digest");if(!["pass","fail"].includes(input.status))throw new Error("Product QC status must be pass or fail");const ids=Array.isArray(input.evidenceIds)?input.evidenceIds.map(String):[];if(input.status==="pass"&&(!ids.length||ids.some(id=>!current.evidence.some(item=>item?.id===id&&item?.scope==="artifact"&&item?.value?.digest===artifact.digest))))throw new Error("Product QC pass requires exact-digest evidence");const next=clone(current);next.productQc=clone(input);next.factoryStage=input.status==="pass"?"PRODUCT_VERIFIED":"PRODUCT_QC";next.verificationScan=null;next.closeout=null;next.lessons=[];next.audit.push({at:now(),event:"PRODUCT_QC_RECORDED",status:input.status,digest:input.artifactDigest});return next;}
function recordVerificationScanState(current,input={}){if(!["VERIFIED_CHAIN","FIRST_BROKEN_TRUTH"].includes(input.status))throw new Error("unsupported verification scan status");const artifact=currentProductArtifact(current);if(input.status==="VERIFIED_CHAIN"&&(current.factoryStage!=="PRODUCT_VERIFIED"||input.artifactId!==artifact?.id||input.artifactDigest!==artifact?.digest))throw new Error("verified scan must match the current PRODUCT_VERIFIED Artifact");const next=clone(current);next.verificationScan=clone(input);next.factoryStage=input.status==="VERIFIED_CHAIN"?"VERIFIED_CHAIN":"RECOVERY_REQUIRED";next.closeout=null;next.lessons=[];next.audit.push({at:now(),event:"VERIFICATION_SCAN_RECORDED",status:input.status,station:input.station||null});return next;}
function recordCloseoutState(current,input={}){const artifact=currentProductArtifact(current);if(current.verificationScan?.status!=="VERIFIED_CHAIN"||input.status!=="CLOSEOUT_READY"||input.taskId!==current.id||input.finalArtifact?.id!==artifact?.id||input.finalArtifact?.digest!==artifact?.digest)throw new Error("closeout must match the current verified chain and Artifact");const next=clone(current);next.closeout=clone(input);next.factoryStage="CLOSED";next.lessons=[];next.audit.push({at:now(),event:"CLOSEOUT_RECORDED",artifactDigest:artifact.digest});return next;}
function recordLessonState(current,input={}){const artifact=currentProductArtifact(current);if(current.closeout?.status!=="CLOSEOUT_READY"||input.status!=="RECORDED"||input.sourceTaskId!==current.id||input.sourceArtifactDigest!==artifact?.digest)throw new Error("lesson must match the current Artifact and closed task");const id=requiredString(input.id,"lesson id");if(current.lessons.some(i=>i.id===id))throw new Error("duplicate lesson id");const next=clone(current);next.lessons.push(clone(input));next.factoryStage="LEARNED";next.audit.push({at:now(),event:"LESSON_RECORDED",lessonId:id});return next;}
function setWorkbenchTruthState(current,input={}){const next=clone(current);for(const key of ["mission","blueprint","currentPiece"]){if(Object.hasOwn(input,key))next[key]=input[key]==null?null:clone(input[key]);}if(Object.hasOwn(input,"evidence"))next.evidence=Array.isArray(input.evidence)?clone(input.evidence):[];next.audit.push({at:now(),event:"WORKBENCH_TRUTH_UPDATED"});return next;}
function appendAuditState(current,event,details={}){const name=String(event||"").trim();if(!name)throw new Error("audit event is required");const next=clone(current);next.audit.push({...clone(details),at:now(),event:name});return next;}
function wrap(state){const snapshot=()=>{const next=clone(state);next.nextAction=effectiveNextAction(next);return next;};const current=snapshot();return Object.freeze({...current,snapshot,
  transition:(nextState,evidence={})=>wrap(transitionState(state,nextState,evidence)),appendAudit:(event,details={})=>wrap(appendAuditState(state,event,details)),setWorkbenchTruth:(input={})=>wrap(setWorkbenchTruthState(state,input)),setWorkPackage:(input={})=>wrap(setWorkPackageState(state,input)),recordProductionStep:(input={})=>wrap(recordProductionStepState(state,input)),recordPiece:(input={})=>wrap(recordPieceState(state,input)),recordPieceQc:(input={})=>wrap(recordPieceQcState(state,input)),recordGateHandoff:(input={})=>wrap(recordGateHandoffState(state,input)),addEvidence:(input={})=>wrap(addEvidenceState(state,input)),recordAssembly:(input={})=>wrap(recordAssemblyState(state,input)),recordAssemblyQc:(input={})=>wrap(recordAssemblyQcState(state,input)),recordMergeGate:(input={})=>wrap(recordMergeGateState(state,input)),recordBuildArtifact:(input={})=>wrap(recordBuildArtifactState(state,input)),recordSigningGate:(input={})=>wrap(recordSigningGateState(state,input)),recordSignedArtifact:(input={})=>wrap(recordSignedArtifactState(state,input)),recordProductQc:(input={})=>wrap(recordProductQcState(state,input)),recordVerificationScan:(input={})=>wrap(recordVerificationScanState(state,input)),recordCloseout:(input={})=>wrap(recordCloseoutState(state,input)),recordLesson:(input={})=>wrap(recordLessonState(state,input))});}
export function createCodeTask(initial={}){return wrap(normalizeInitial(initial));}
export function createCodeTaskFromSnapshot(snapshot){return wrap(normalizeSnapshot(snapshot));}
