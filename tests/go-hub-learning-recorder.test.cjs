"use strict";
const test=require("node:test"); const assert=require("node:assert/strict"); const path=require("node:path"); const {pathToFileURL}=require("node:url"); const file=pathToFileURL(path.resolve(__dirname,"..","go-hub-learning-recorder.js")).href;
test("learning recorder creates a compact source-bound reusable lesson",async()=>{const {recordLesson}=await import(file);const lesson=recordLesson({id:"lesson-1",context:"Assembly QC failed at a stale head",action:"scan exact heads",finding:"piece evidence was current but assembly evidence was stale",resolution:"rerun Assembly QC",reusableWhen:"an integration head changes",sourceTaskId:"task-1",sourceArtifactDigest:"digest-1",recordedAt:"2026-09-14T13:00:00.000Z"});assert.equal(lesson.status,"RECORDED");assert.equal(lesson.sourceArtifactDigest,"digest-1");assert.equal(Object.isFrozen(lesson),true);});
test("learning recorder rejects incomplete generic notes",async()=>{const {recordLesson}=await import(file);assert.throws(()=>recordLesson({id:"x",context:"x"}),/action is required/);});
