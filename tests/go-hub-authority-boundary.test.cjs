"use strict";
const test=require("node:test");const assert=require("node:assert/strict");const path=require("node:path");const {pathToFileURL}=require("node:url");const root=path.resolve(__dirname,"..");
test("V4 decision concerns have exactly one active authority",async()=>{const m=await import(pathToFileURL(path.join(root,"go-hub-authority-map.js")).href);const owners=m.assertUniqueDecisionAuthority();assert.equal(owners.WORK_IDENTITY,"centre");assert.equal(owners.WORK_PERSISTENCE,"centre");assert.equal(owners.WORK_REGISTRY,"heimdall");assert.equal(owners.PASS,"heimdall");assert.equal(owners.PROJECT_BOARD,"heimdall");assert.equal(owners.FACTORY_LIFECYCLE,"factory");assert.equal(owners.MAINTENANCE_MAP,"maintenance");assert.equal(owners.KNOWLEDGE_EXCHANGE,"counter");assert.equal(owners.AUDIT_HISTORY,"audit");});
test("V4 has no Heimdall Evidence Gate, RCA, or repair authority",async()=>{const m=await import(pathToFileURL(path.join(root,"go-hub-authority-map.js")).href);for(const retired of m.RETIRED_AUTHORITY_NAMES)assert.equal(m.authorityOwner(retired),null,retired);});
test("Factory and Maintenance remain checker/probe owners, not Evidence authorities",async()=>{const m=await import(pathToFileURL(path.join(root,"go-hub-authority-map.js")).href);const map=m.authorityMap();assert.ok(map.factory.checks.includes("PIECE_QC"));assert.ok(map.factory.checks.includes("ASSEMBLY_QC"));assert.ok(map.maintenance.checks.includes("FIRST_BREAK"));assert.equal(map.maintenance.decisionAuthority.includes("RCA"),false);});
